{
  "name": "webside-epstin",
  "version": "1.0.0",
  "description": "A website for documents, PDFs, text, and pictures",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "npm run build && gh-pages -d public"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
